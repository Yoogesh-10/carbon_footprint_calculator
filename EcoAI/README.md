# Carbon Footprint Calculator

Project structure:

- `frontend/` — Vite + React frontend. Deploy this folder to Vercel.
- `backend/` — Python backend. Deploy this folder to Render.

Do not move the frontend files out of the `frontend` folder.
